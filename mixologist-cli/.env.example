ANTHROPIC_API_KEY=
# Optional: path to MCP_Server dist (default: sibling MCP_Server/dist/index.js)
# MCP_SERVER_PATH=
# HTTP server port when using npm run start:http (default 4100)
# MIXOLOGIST_HTTP_PORT=4100
