/**
 * Node.js API and LangChain orchestration (future).
 * Call into MCP_Server tools from here or expose HTTP for the Next.js frontend.
 */
function main(): void {
  console.info("[backend] Ready — add LangChain, routes, and MCP clients here.");
}

main();
