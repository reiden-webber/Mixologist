{
  "name": "bartender",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "MCP_Server"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "build:all": "npm run build -w backend && npm run build -w mcp-server && npm run build -w frontend",
    "lint": "npm run lint -w frontend"
  }
}
